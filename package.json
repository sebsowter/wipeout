{
  "scripts": {
    "deploy": "npx vite build && gh-pages -d dist"
  },
  "dependencies": {
    "@types/three": "^0.161.2",
    "three": "^0.161.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "gh-pages": "^6.1.1",
    "vite": "^5.1.3"
  }
}
